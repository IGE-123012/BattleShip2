version: "1.0"

# 1. Aqui forçamos a usar a versão Community (gratuita) que não pede tokens
linter: jetbrains/qodana-jvm-community

profile:
  name: qodana.starter

projectJDK: "25"

# 2. Aqui ativamos a inspeção específica para o teu cheiro no código
inspections:
  - inspection: OverlyLongMethod
    enabled: true